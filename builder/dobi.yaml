# ===================================================
# images
# ===================================================
image=image-cplusplus-builder:
  image: "{env.BB_DOCKER_NAMESPACE:elbb}/bb-cplusplus-builder"
  context: builder
  tags: ["{env.GitVersion_BranchVersion}"]

# ===================================================
# alias
# ===================================================

alias=cplusplus-builder-build:
  tasks:
    - image-cplusplus-builder

alias=cplusplus-builder-deploy:
  tasks:
    - image-cplusplus-builder:push
